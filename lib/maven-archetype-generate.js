'use strict';
// var debug = require('debug')('generator-alfresco:maven-archetype-generate');
// var constants = require('./constants.js');

module.exports = function (yo) {
  var module = {};

  module.generate = function () {
    return true;
  };

  return module;
};

// vim: autoindent expandtab tabstop=2 shiftwidth=2 softtabstop=2
